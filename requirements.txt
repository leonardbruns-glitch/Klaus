# ── Core (required for dry-run simulation) ───────────────────────────────────
aiohttp>=3.9.0
python-dotenv>=1.0.0

# ── Live trading only (not needed for dry-run) ───────────────────────────────
# Uncomment when you have Polymarket credentials and are ready to go live.
# py-clob-client and web3 7.x share compatible hexbytes>=1.2.0
#
# py-clob-client>=0.14.0
# web3>=7.0.0
